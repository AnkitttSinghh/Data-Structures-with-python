# Data-Structures-with-python (int, float, strings, boolean, complex, list, tuple, set, dictionary)

# LIST[1,2,3,4]: read/ write data type
1) what is a List: python datastructure / mutable/ heterogenous/homogenouos/ referential array
2) List vs Array 
3)  create
4)  acsess
5)  Edit
6)  Add (append() : item added at last, insert(): add item at any index, extend(): to insert multiple items at the end) 
7)  Delete ( del: delete entire list/or any item with indexes,remove(): remove any element, pop(): pop last element, clear(): it empties the list)
8)  operations ( +: concatenation, *: multiplication, for loop, while loop, etc)
9)  Functions ( len(),min(), max(), sorted())

# TUPLES(1,2,(1,2),3,4): Read only data type
1) create: check to create sinle item tuple
2) access: exactly similiar to list 
3) slicing: as it is in list
4) edit: tuples are immutable
5) deleting: can only delete whole tuple
6) operations: (cocatenate, multiply, loop, mebership operator(in, not in))
7) fucntions: len, min, max, sum, sorted(tuple, reverse= true/false)

# SETS (1,2,3,4)
1) do not allow duplicates
2) sets have no indexing/slicing
3) sets do not allow mutable datatype[list]
4) set itself is a mutable datatype thus 2d/3d sets are not alllowed

# Dictionary: {Key: Value} 
1) No indexes/ No slicing
2) Mutable Datatype
3) kes are immutable, values are immutable
4) keys should be unique
5) i) how to access dictionary
     mydict = {"name": "ankit, "marks" : 100}
     mydict['name'] = ankit
   ii) how to edit:
     mydict["name"] = "mayank
   iii) how to add new key
     mydict["new key"] = 1223
   iv) how to delete/ clear
        del mydict
        mydict.clear() #it empties the dictionary
   v) operations
        no concatenation/ no multiplicaion/ looping/ membership( for keys)
   
   
       
   




   
